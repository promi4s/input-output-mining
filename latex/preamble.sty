\ProvidesPackage{preamble}

%-------------------
% Math
%-------------------
\RequirePackage{amsmath}
\RequirePackage{amssymb}
\RequirePackage{amsthm}
\RequirePackage{mathtools}

% Number sets
\newcommand*{\B}{\lbrace 0,1\rbrace}
\newcommand*{\N}{\mathbb N}
\newcommand*{\Z}{\mathbb Z}
\newcommand*{\Q}{\mathbb Q}
\newcommand*{\R}{\mathbb R}

% Mathematical symbols
\newcommand*{\pto}{\rightharpoonup}
\DeclareMathOperator*{\dom}{dom}
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}
\newcommand*{\coloniff}{\;\vcentcolon\mathrel{\mkern-5mu}\iff}
\newcommand*{\iffcolon}{\iff\mathrel{\mkern-5mu}\vcentcolon\;}
\DeclarePairedDelimiter\abs\lvert\rvert
\DeclarePairedDelimiter\seq\langle\rangle
\providecommand\given{}
\newcommand\SetSymbol[1][]{%
	\nonscript\:#1\vert
	\allowbreak
	\nonscript\:
	\mathopen{}}
\DeclarePairedDelimiterX\Set[1]\lbrace\rbrace{%
	\renewcommand\given{\SetSymbol[\delimsize]}
	#1
}
\DeclarePairedDelimiterX\MultiSet[1]\lbrack\rbrack{%
	\renewcommand\given{\SetSymbol[\delimsize]}
	#1
}
\DeclarePairedDelimiterXPP{\lang}[1]{\mathcal L}{(}{)}{}{#1}

% Process Mining specific symbols
\newcommand*{\pset}{{\bullet}}
\newcommand*{\nomove}{{\gg}}
\DeclarePairedDelimiter{\fire}{\lbrack}{\rangle}
\DeclarePairedDelimiter{\reach}{\lbrack}{\rangle}

% Theorem styles
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}{Corollary}[theorem]
\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]
\theoremstyle{remark}
\newtheorem*{remark}{Remark}


%-------------------
% Lists and Tables
%-------------------
\RequirePackage{enumitem}
\RequirePackage{booktabs}
\RequirePackage{array}
\RequirePackage{multirow}


%-------------------
% Graphics
%-------------------
\RequirePackage[subrefformat=parens]{subcaption}


%-------------------
% Citations and References
%-------------------
\RequirePackage[autostyle=true]{csquotes}
\RequirePackage[
	backend=biber,
	style=numeric-comp,
	sorting=nyvt,
	sortcites=true,
	uniquename=init,
	giveninits=true,
	maxbibnames=99,
	maxcitenames=2,
	minnames=1,
	%isbn=false,
	%url=false,
]{biblatex}
\DeclareNameAlias{sortname}{family-given}
\DeclareSourcemap{
	\maps[datatype=bibtex]{
		\map[overwrite]{
			\step[fieldsource=doi, final]
			\step[fieldset=issn, null]
			\step[fieldset=url, null]
			\step[fieldset=eprint, null]
		}
	}
}\DeclareFieldFormat{eprint:urn}{%
	URN\addcolon\space
	\ifhyperref
	{\href{http://www.nbn-resolving.org/#1}{\nolinkurl{#1}}}
	{\nolinkurl{#1}}
}


%-------------------
% Links
%-------------------
\RequirePackage{hyperref}
\RequirePackage[noabbrev,capitalize]{cleveref}


%-------------------
% Author Support
%-------------------
\RequirePackage{todonotes}


\endinput
